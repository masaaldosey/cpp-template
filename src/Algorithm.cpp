#include "Algorithm.h"

/**
 * @brief Sum two numbers and return the result.
 *
 * @param num_1 First operand.
 * @param num_2 Second operand.
 * @return double Result of `num_1` + `num_2`.
 */
double AddTwoNumbers(double num_1, double num_2)
{

	return num_1 + num_2;
}
